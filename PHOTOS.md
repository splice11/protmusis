# Photos — status

All photos have been collected, renamed, and converted (max 1600 px JPEG;
the enlargement map as PNG). They live in `photos/` and are wired into the
answer slides by `generate_slides.py`.

See **[photos/CREDITS.md](photos/CREDITS.md)** for what each file shows,
which slide uses it, and the reconstructed attribution — including the
items still marked *to confirm*.

Only the Round 1 Q5 answer (the Umaru Dikko crate) still uses an
illustration instead of a photo — press photos of that story are
agency-owned; the slide links to the video instead.
